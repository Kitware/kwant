/*ckwg +5
 * Copyright 2011 by Kitware, Inc. All Rights Reserved. Please refer to
 * KITWARE_LICENSE.TXT for licensing information, or contact General Counsel,
 * Kitware, Inc., 28 Corporate Drive, Clifton Park, NY 12065.
 */

// note: there are intentionally no include guards on this file.
// it gets included twice (with suitable definitions of the EVENT_NAME
// macro) to generate the event_types enum and corresponding string names
// you should not change the order of these event_types.  Add new ones
// before "OTHER"
EVENT_NAME(BASE),
EVENT_NAME(VEHICLE),
EVENT_NAME(HUMAN),
EVENT_NAME(STOPPING),
EVENT_NAME(STARTING),
EVENT_NAME(TURNING),
EVENT_NAME(UTURNING),
EVENT_NAME(FOLLOWING),
EVENT_NAME(MEETING),
EVENT_NAME(AIMLESS_DRIVING),
EVENT_NAME(PASSING),
EVENT_NAME(PARALLEL),
EVENT_NAME(RESTORED_ORDER),
EVENT_NAME(FLIP_FLOP),
EVENT_NAME(KEEP_DISTANCE_CLOSE),
EVENT_NAME(KEEP_DISTANCE_FAR),
EVENT_NAME(VARY_DISTANCE),
EVENT_NAME(ACCELERATING),
EVENT_NAME(DECELERATING),
EVENT_NAME(DRIVING_FAST),
EVENT_NAME(DRIVING_SLOW),
EVENT_NAME(APPROACH),
EVENT_NAME(RETREAT),
EVENT_NAME(APPEAR),
EVENT_NAME(DISAPPEAR),
EVENT_NAME(STATIONARY),
EVENT_NAME(DIRECTION),
EVENT_NAME(REENTRY),
EVENT_NAME(ANOMALOUS),
EVENT_NAME(HONEYWELL_ANOMALOUS),
EVENT_NAME(BUS_STOP_NODE),
EVENT_NAME(PARALLEL_FAR),
EVENT_NAME(TRACK_LINKING),
EVENT_NAME(DRIVING),
EVENT_NAME(TRACK_GROUP),
EVENT_NAME(EXIT_BUILDING),
EVENT_NAME(ENTER_BUILDING),
EVENT_NAME(EXIT_VEHICLE),
EVENT_NAME(ENTER_VEHICLE),
EVENT_NAME(NEAR_VEHICLE),
EVENT_NAME(WALKING),
EVENT_NAME(STANDING),
EVENT_NAME(RUNNING),
EVENT_NAME(DUMMY_ONE),
EVENT_NAME(DUMMY_TWO),
EVENT_NAME(DUMMY_THREE),
EVENT_NAME(DUMMY_FOUR),
EVENT_NAME(OTHER),
EVENT_NAME(MAX) /* note: MAX must always be last */
